---
- hosts: wiki
  tasks:

  - name: install zabbix-agent repository
    apt_repository: repo='deb http://repo.zabbix.com/zabbix/2.4/debian jessie main' state=present

  - name: update apt cache
    apt: update_cache=yes

  - name: install zabbix-agent
    apt: name=zabix-agent
  
  - replace: dest=/etc/zabbix/zabbix-agentd.conf regexp='(Server=\d+.\d+.\d+.\d+)' validate='' 
  
